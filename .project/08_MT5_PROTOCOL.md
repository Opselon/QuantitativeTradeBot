# 08_MT5_PROTOCOL – Nexus MT5 Bridge Architecture

## 1. Overview

This document specifies how the Nexus Trading Engine integrates with MetaTrader 5 (MT5) via a dedicated, real TCP bridge:

- **Nexus (.NET / C#)** is the *brain* (strategies, risk, portfolio, orchestration).
- **MetaTrader 5 Terminal** is the broker client.
- **`NexusBridge.mq5` Expert Advisor (EA)** inside MT5 is the *executor* that:
  - Receives commands from Nexus over a bridge (TCP/WebSocket/etc.).
  - Calls MT5 APIs to read account/positions/orders and place trades.
  - Sends structured JSON responses back to Nexus.

The communication model:

```text
[Nexus C#]  <== JSON over TCP ==>  [NexusBridge.mq5 EA inside MT5]  <===> [MT5 Server/Broker]
```

The goal is to have a **production-ready MT5 connectivity path** that coexists with the existing **simulated MT5 adapter**, respecting clean architecture boundaries and DDD/CQRS principles.

---

## 2. File & Project Structure

### 2.1. Nexus (.NET / C#) Side

The physical implementation of the MT5 real bridge and routing architecture is integrated as follows:

```text
src/
  Nexus.Application/
    Mt5Bridge/
      Contracts/
        BridgeMessageEnvelope.cs    // Unified JSON wrapper
        BridgeError.cs              // Redacted, safe error structure
        BridgeOrderSide.cs          // Enum for order types (Buy/Sell)
        BridgePositionSide.cs       // Enum for position types (Buy/Sell)
        BridgeOrderExecutionStatus.cs // Enum for execution statuses
        PlaceOrderRequest.cs        // Order placement request payload
        PlaceOrderResponse.cs       // Order placement response payload
        ClosePositionRequest.cs     // Position closure request payload
        ClosePositionResponse.cs    // Position closure response payload
        GetOpenPositionsRequest.cs  // Request to list open positions
        GetOpenPositionsResponse.cs // Response containing the list of open positions
        BridgePositionDto.cs        // Position information DTO
        GetAccountSnapshotRequest.cs
        GetAccountSnapshotResponse.cs
        PingRequest.cs
        PingResponse.cs

    Ports/
      IMt5BridgeClient.cs           // Abstraction layer over TCP socket client
      IMt5AccountService.cs
      IMt5ConnectionService.cs
      IMt5TradeService.cs           // Abstraction layer for trade execution
      IMt5Session.cs

  Nexus.Infrastructure/
    Mt5Bridge/
      TcpMt5BridgeClient.cs         // Direct TCP socket server/listener routing JSON
    Adapters/
      Mt5/
        RealMt5BridgeSession.cs     // Custom session wrapping TcpMt5BridgeClient
        RealMt5BridgeAdapter.cs     // Maps JSON payloads to Application Snapshots / Trades
        RealMt5BridgeConnectionService.cs // Handles connection checks & handshakes with EA
        SimulatedMt5TradeService.cs // In-memory simulated position storage and trade execution
        RoutingMt5AccountService.cs // Dynamic delegate choosing Simulated vs Real mode
        RoutingMt5ConnectionService.cs // Dynamic delegate choosing Simulated vs Real mode
        RoutingMt5TradeService.cs   // Dynamic delegate choosing Simulated vs Real trade mode

tests/
  Nexus.Tests.Unit/
    Desktop/
      Mt5BridgeTests.cs             // Verification suite for serialization & routing
```

### 2.2. MetaTrader 5 (MQL5) Side

Primary MQL5 file:

- `MQL5/Experts/Nexus/NexusBridge.mq5` – an Expert Advisor attached to a chart (e.g. EURUSD M1) which listens on a port, parses requests, executes trading orders, and responds.

---

## 3. Bridge Message Contracts (JSON)

All messages exchanged between Nexus and `NexusBridge.mq5` use a unified JSON envelope.

### 3.1. Envelope Structure

Logical (language-agnostic) format:

```json
{
  "messageType": "Request",   // or "Response"
  "requestId": "GUID-or-string",
  "command": "GetAccountSnapshot",  // or "Ping", "PlaceOrder", "ClosePosition", "GetOpenPositions"
  "payload": { /* command-specific data */ },
  "error": {
    "code": "…",
    "message": "…"
  },
  "version": "1.0"
}
```

Fields:

- `messageType`
  - `"Request"`: Nexus → MT5
  - `"Response"`: MT5 → Nexus
- `requestId`
  - Generated by Nexus (e.g., GUID).
  - Echoed by MQL5 in the response for correlation.
- `command`
  - Logical command name (`GetAccountSnapshot`, `Ping`, `PlaceOrder`, `ClosePosition`, `GetOpenPositions`).
- `payload`
  - Request or response data specific to the command.
- `error`
  - `null` for success.
  - Object on failure: includes `code`, `message`, and optional extra details.

### 3.2. Example – `GetAccountSnapshot`

Request:

```json
{
  "messageType": "Request",
  "requestId": "8bf3a207-7e5e-4eb2-a3c0-8f57ea123456",
  "command": "GetAccountSnapshot",
  "payload": null,
  "error": null,
  "version": "1.0"
}
```

Response:

```json
{
  "messageType": "Response",
  "requestId": "8bf3a207-7e5e-4eb2-a3c0-8f57ea123456",
  "command": "GetAccountSnapshot",
  "payload": {
    "accountId": 12345678,
    "broker": "MyBroker-Live",
    "currency": "USD",
    "balance": 10000.50,
    "equity": 10050.75,
    "margin": 500.00,
    "freeMargin": 9550.75,
    "leverage": 100,
    "connectionHealth": "Healthy"
  },
  "error": null,
  "version": "1.0"
}
```

---

## 4. New Core Trading Protocols

These trading protocols are fully implemented and integrated across both C# (.NET) and MQL5 (MT5 Terminal) sides:
- **C# Port Method**: `IMt5TradingService` sends JSON TCP requests via `TcpMt5BridgeClient`.
- **MQL5 Terminal Side**: `NexusBridge.mq5` parses incoming commands, executes them with robustness safeguards (normalizations, filling mode fallbacks), and replies with structured responses.

### 4.1. `PlaceOrder` Command

Launches a market execution order inside the broker terminal.

- **C# Port Method**: `IMt5TradingService.PlaceMarketOrderAsync(symbol, side, volume, stopLoss, takeProfit, comment, clientCorrelationId, cancellationToken)`
- **C# Request DTO Shape**: `PlaceOrderRequest`
- **C# Response DTO Shape**: `PlaceOrderResponse` (mapped to application-level `PlaceOrderResult`)

#### 4.1.1. Request Payload

```json
{
  "messageType": "Request",
  "requestId": "req-999-place",
  "command": "PlaceOrder",
  "payload": {
    "symbol": "EURUSD",
    "side": "Buy",          // "Buy" or "Sell"
    "volume": 0.10,
    "stopLoss": 1.08000,    // Optional SL price
    "takeProfit": 1.10000,  // Optional TP price
    "comment": "Algo Order 1",
    "clientCorrelationId": "strat-abc-001"
  },
  "error": null,
  "version": "1.0"
}
```

#### 4.1.2. Successful Response Payload

```json
{
  "messageType": "Response",
  "requestId": "req-999-place",
  "command": "PlaceOrder",
  "payload": {
    "success": true,
    "ticket": 58923412,
    "status": "Executed",
    "brokerMessage": "Trade completed successfully.",
    "comment": "Algo Order 1"
  },
  "error": null,
  "version": "1.0"
}
```

#### 4.1.3. Failed Response Payload

```json
{
  "messageType": "Response",
  "requestId": "req-999-place",
  "command": "PlaceOrder",
  "payload": {
    "success": false,
    "ticket": 0,
    "status": "Failed",
    "brokerMessage": "OrderSend failed. Retcode: 10013, Error: Invalid Volume.",
    "comment": ""
  },
  "error": {
    "code": "TRADE_REJECTED",
    "message": "OrderSend failed. Retcode: 10013, Error: Invalid Volume."
  },
  "version": "1.0"
}
```

---

### 4.2. `ClosePosition` Command

Requests a closure (or partial close) of a designated open position ticket.

- **C# Port Method**: `IMt5TradingService.ClosePositionAsync(positionTicket, symbol, volume, cancellationToken)`
- **C# Request DTO Shape**: `ClosePositionRequest`
- **C# Response DTO Shape**: `ClosePositionResponse` (mapped to application-level `ClosePositionResult`)

*Note: Following architectural guidelines, passing the symbol directly eliminates the need for sequential nested network round-trips over the bridge to query the position’s symbol, maximizing pipeline performance.*

#### 4.2.1. Request Payload

```json
{
  "messageType": "Request",
  "requestId": "req-999-close",
  "command": "ClosePosition",
  "payload": {
    "ticket": 58923412,
    "symbol": "EURUSD",
    "volume": null          // null for full position closure
  },
  "error": null,
  "version": "1.0"
}
```

#### 4.2.2. Successful Response Payload

```json
{
  "messageType": "Response",
  "requestId": "req-999-close",
  "command": "ClosePosition",
  "payload": {
    "success": true,
    "ticket": 58923412,
    "brokerMessage": "Position closed successfully."
  },
  "error": null,
  "version": "1.0"
}
```

---

### 4.3. `GetOpenPositions` Command

Downloads the current list of active, open positions from the MT5 broker terminal.

- **C# Port Method**: `IMt5TradingService.GetOpenPositionsAsync(cancellationToken)`
- **C# Request DTO Shape**: `GetOpenPositionsRequest` (empty request body)
- **C# Response DTO Shape**: `GetOpenPositionsResponse` (mapped to list of application-level `OpenPositionDto` values)

#### 4.3.1. Request Payload

```json
{
  "messageType": "Request",
  "requestId": "req-999-positions",
  "command": "GetOpenPositions",
  "payload": null,
  "error": null,
  "version": "1.0"
}
```

#### 4.3.2. Response Payload

```json
{
  "messageType": "Response",
  "requestId": "req-999-positions",
  "command": "GetOpenPositions",
  "payload": {
    "positions": [
      {
        "ticket": 58923412,
        "symbol": "EURUSD",
        "side": "Buy",
        "volume": 0.10,
        "openPrice": 1.08500,
        "currentPrice": 1.08550,
        "stopLoss": 1.08000,
        "takeProfit": 1.10000,
        "profit": 5.00,
        "swap": 0.00,
        "magicNumber": 123456,
        "comment": "Algo Order 1",
        "openTime": "2025-05-20T12:00:00Z"
      }
    ]
  },
  "error": null,
  "version": "1.0"
}
```

---

## 5. MQL5 – `NexusBridge.mq5` Handler Responsibilities

1. **JSON Property Extraction**: Features robust extraction helper methods (`GetJsonStringValue`, `GetJsonDoubleValue`, and `GetJsonIntValue`) capable of reading both quoted strings and unquoted numeric values inside payload trees.
2. **Trade API Validation**: Assures essential properties (Symbol, Volume, and Type) are fully populated and selectable on-terminal.
3. **MqlTradeRequest Execution**: Prepares trading requests matching MT5 execution filling policies and dispatches via native `OrderSend()`.
4. **Robustness and Normalizations (Stage 2 improvements)**:
   - **Dynamic Volume step rounding**: Rounds requested trade lots to the broker's specific `SYMBOL_VOLUME_STEP` and forces them between `SYMBOL_VOLUME_MIN` and `SYMBOL_VOLUME_MAX`.
   - **Dynamic Filling Mode Fallbacks**: Detects supported filling modes (`FOK` vs `IOC` vs `RETURN`) on the specific symbol dynamically to avoid filling-mode rejection codes (e.g., retcode 10030).
   - **Precision Pricing Fallbacks**: Resolves latest symbol ask/bid prices via native high-precision `SymbolInfoTick` triggers, falling back cleanly to `SymbolInfoDouble` if necessary. Normalizes trade stop-loss and take-profit prices based on symbol's exact decimal digits (`SYMBOL_DIGITS`).
5. **Structured Error Dispatching**: Maps terminal rejections to standardized bridge error codes (such as `TRADE_REJECTED`, `INVALID_PAYLOAD`, and `POSITION_NOT_FOUND`).
6. **Escape Serialization Safety**: Safely escapes string outputs to prevent malformed or broken JSON transfers.

---

## 6. Sequence Flows

### 6.1. Place Order Sequence

```text
[Nexus Brain C#]                 [TcpBridge]                 [NexusBridge EA]
       |                              |                             |
       |-- PlaceOrderRequest ------>  |-- Forward request ------->  |
       |   (requestId: "req-123")     |                             |-- Validate & OrderSend()
       |                              |                             |-- Query trade result
       |                              |  <-- PlaceOrderResponse ----|
       |  <-- Correlate & Deserialize-|      (ticket: 58923412)     |
```

### 6.2. Position Synchronization Sequence

```text
[Nexus Brain C#]                 [TcpBridge]                 [NexusBridge EA]
       |                              |                             |
       |-- GetOpenPositionsRequest -> |-- Forward request ------->  |
       |                              |                             |-- Enumerate active positions
       |                              |                             |-- Format list to JSON Array
       |                              |  <-- Positions JSON Array --|
       |  <-- Map & Update UI --------|                             |
```

---

## 7. Mode Selection – Simulated vs Real Bridge

The C# engine supports dual-mode execution:

### 7.1. Configuration
```json
{
  "SelectedProvider": "PostgreSQL",
  "Mt5Mode": "Real",
  "Mt5BridgeHost": "127.0.0.1",
  "Mt5BridgePort": 5000,
  "Mt5BridgeUseSsl": false
}
```

### 7.2. Routing
DI registrations dynamically load `RoutingMt5ConnectionService`, `RoutingMt5AccountService`, and `RoutingMt5TradeService`.
Calls are automatically delegated to the active adapter:
- `SimulatedMt5ConnectionService` / `SimulatedMt5AccountService` / `SimulatedMt5TradeService` (for offline dev/evaluation).
- `RealMt5BridgeConnectionService` / `RealMt5BridgeAdapter` (for real bridge execution).

---

## 8. Testing & Local Verification

- **Contracts Test Suite**: Verifies round-trip JSON serialization of envelopes and payloads.
- **Mapping Verification**: Confirms account snaps map accurately to Clean Architecture domain entities.
- **Routing Verification**: Ensures dynamic delegation switching functions seamlessly.
- **Regression Checks**: Validates that mock workflows are preserved without interference.

---

## 9. UI Integration and Message Flows

With Stage 3, the manual trading operator dashboard (`Mt5TradingPanel.xaml`) has been fully integrated into the Desktop Workstation.

### 9.1. Architectural Layout
The UI is strictly separated from any physical infrastructure:
```text
[Mt5TradingPanel View (XAML)]
             │
             ▼
[Mt5TradingViewModel]
             │
             ▼
[IMt5OperatorService (Facade)]
             │
             ▼
[IMt5TradingService (Abstraction)] ──► [RoutingMt5TradingService]
                                                 │
                        ┌────────────────────────┴────────────────────────┐
                        ▼                                                 ▼
          [SimulatedMt5TradingService]                      [RealMt5TradingService]
```

### 9.2. End-to-End Message Flows

#### 1. Retrieve & Refresh Positions
- **Trigger**: Manual operator clicks "REFRESH POSITIONS" or the auto-refresh background loop timer fires (every 5 seconds).
- **Flow**:
  1. `Mt5TradingViewModel` calls `_operatorService.GetPositionsAsync()`.
  2. `Mt5OperatorService` invokes `_tradingService.GetOpenPositionsAsync()`.
  3. `RoutingMt5TradingService` resolves the active service:
     - **Simulated Mode**: Fetches in-memory simulated positions from `SimulatedMt5TradeService`.
     - **Real Mode**: Sends a `GetOpenPositions` JSON Request Envelope over TCP via `TcpMt5BridgeClient` to `NexusBridge.mq5`.
  4. The response flows back, gets converted into clean `DesktopPositionDto` items, and wraps in `DesktopPositionViewModel` to bind to the UI Grid on the main Dispatcher Thread.

#### 2. Execute Market Order (Buy/Sell)
- **Trigger**: Operator validates Symbol & Volume, then clicks "BUY MARKET" or "SELL MARKET".
- **Flow**:
  1. `Mt5TradingViewModel` executes `PlaceOrderAsync(symbol, side, volume)`.
  2. `Mt5OperatorService` maps side to `BridgeOrderSide` and calls `_tradingService.PlaceMarketOrderAsync()`.
  3. Active trade service compiles request and returns trade results (`Ticket`, `Success`, `ErrorMessage`).
  4. Operator service normalizes exceptions (e.g., `SocketException` mapped to "Connection lost") and returns `DesktopTradeResult`.
  5. UI updates StatusMessage/ErrorMessage and triggers an automated positional refresh.

#### 3. Close Active Position
- **Trigger**: Operator selects a row in the Grid and clicks "CLOSE POSITION".
- **Flow**:
  1. `Mt5TradingViewModel` dispatches `ClosePositionAsync(ticket, symbol)`.
  2. `Mt5OperatorService` invokes `_tradingService.ClosePositionAsync()`.
  3. Active implementation instructs close execution (on simulated positions or real terminal tickets).
  4. Results are processed, errors are normalized, and UI refreshes the active positions table.

---

## 10. New Real-Time Handshake, Subscription & Tick Streaming Commands (Stage B additions)

To support real-time high-precision market watch widgets, live terminal verification, and native Core math acceleration, the bridge exposes new JSON protocol frames:

### 10.0. `Handshake` Command

Triggered immediately upon socket connection.

Request Payload:
```json
{
  "messageType": "Request",
  "requestId": "req-handshake-01",
  "command": "Handshake",
  "payload": {
    "sessionId": "guid-test-session",
    "protocolVersion": "1.0",
    "clientIdentifier": "NTE-Desktop"
  }
}
```

---

## 11. Security and Sensitive Data Protection

To protect the integrity of live trading accounts and prevent unauthorized access, the bridge enforces the following security standards:

1. **Secure Memory-Only Login Flows**: Sensitive broker passwords and login session details are never written to disk or configuration files. They are handled only in transient memory during active session lifetimes.
2. **Local WindowsSecretStore Integration**: When credentials must be persisted across desktop re-runs, NTE utilizes the `WindowsSecretStore` (relying on DPAPI) on Windows, falling back to machine-bound file encryption on non-Windows/Linux environments.
3. **Log Redaction & Sanitization**: All incoming/outgoing payloads and diagnostic messages flowing through the `DiagnosticRingBuffer` are automatically sanitized via the `LogSanitizer` utility, masking tokens, API keys, passwords, and other credentials with `******` strings before they hit logs or are queried via the Kestrel REST APIs.
4. **Localhost API Binding & Authentication**: The Kestrel minimal API server operates strictly bound to the local interface (`127.0.0.1:5005`) to prevent external network exploitation. All mutating routes enforce bearer-style token validation via the `X-Nexus-Token` header.

Response Payload:
```json
{
  "messageType": "Response",
  "requestId": "req-handshake-01",
  "command": "Handshake",
  "payload": {
    "eaName": "NexusBridge",
    "eaVersion": "1.00",
    "accountId": 7820491,
    "brokerServer": "ICMarkets-Demo",
    "subscribedSymbols": ["EURUSD"],
    "isInitialized": true,
    "chartSymbol": "EURUSD"
  }
}
```

### 10.1. `Login` Command

Sent by C# to authorize the MT5 session with matching account identifiers.

Request Payload:
```json
{
  "messageType": "Request",
  "requestId": "req-login-01",
  "command": "Login",
  "payload": {
    "accountId": "7820491",
    "password": "demo_password",
    "brokerServer": "ICMarkets-Demo"
  }
}
```

Response Payload:
```json
{
  "messageType": "Response",
  "requestId": "req-login-01",
  "command": "Login",
  "payload": {
    "success": true,
    "errorMessage": ""
  }
}
```

### 10.2. `SubscribeSymbol` / `UnsubscribeSymbol` Commands

Used to add or remove instruments from the EA's high-frequency tick watch list.

Request Payload:
```json
{
  "messageType": "Request",
  "requestId": "req-sub-01",
  "command": "SubscribeSymbol",
  "payload": {
    "symbol": "EURUSD"
  }
}
```

Response Payload:
```json
{
  "messageType": "Response",
  "requestId": "req-sub-01",
  "command": "SubscribeSymbol",
  "payload": {
    "success": true,
    "errorMessage": ""
  }
}
```

### 10.3. `ReceiveTickStream` Broadcast

Streamed automatically by `NexusBridge.mq5` whenever any of the active symbols receive a bid/ask price modification.

Notification Payload:
```json
{
  "messageType": "Request",
  "requestId": "tick-EURUSD-timestamp",
  "command": "ReceiveTickStream",
  "payload": {
    "symbol": "EURUSD",
    "timestamp": "2025-05-20T12:00:05Z",
    "bid": 1.08500,
    "ask": 1.08510,
    "spread": 0.00010,
    "volume": 1.0
  }
}
```

---

## 11. Security and Sensitive Data Protection

To protect the integrity of live trading accounts and prevent unauthorized access, the bridge enforces the following security standards:

1. **Secure Memory-Only Login Flows**: Sensitive broker passwords and login session details are never written to disk or configuration files. They are handled only in transient memory during active session lifetimes.
2. **Local WindowsSecretStore Integration**: When credentials must be persisted across desktop re-runs, NTE utilizes the `WindowsSecretStore` (relying on DPAPI) on Windows, falling back to machine-bound file encryption on non-Windows/Linux environments.
3. **Log Redaction & Sanitization**: All incoming/outgoing payloads and diagnostic messages flowing through the `DiagnosticRingBuffer` are automatically sanitized via the `LogSanitizer` utility, masking tokens, API keys, passwords, and other credentials with `******` strings before they hit logs or are queried via the Kestrel REST APIs.
4. **Localhost API Binding & Authentication**: The Kestrel minimal API server operates strictly bound to the local interface (`127.0.0.1:5005`) to prevent external network exploitation. All mutating routes enforce bearer-style token validation via the `X-Nexus-Token` header.

---

## 12. Bridge Lifecycle & Telemetry Flow States

To support live, stateful monitoring of the MetaTrader 5 Bridge session, NTE enforces a multi-state connection tracking flow:

- **Stopped**: The bridge server listening socket is offline.
- **Listening**: The local NTE server socket listener has successfully bound to `127.0.0.1:5000` (breathing blue glow in the user workspace).
- **SocketConnected**: A raw TCP socket handshake attempt is established from the `NexusBridge.mq5` client (pulsing orange).
- **Handshaking**: Handshake payload dispatched, waiting for the active MQL5 Expert Advisor to respond.
- **HandshakeSuccess**: Bidirectional handshake complete, active chart metadata and terminal broker specifications mapped (breathing green).
- **Authenticated**: Trade credentials checked and verified with the active MT5 broker terminal, manual and automated trading desk execution pipelines unlocked.
- **ConnectionError**: Socket transport or handshake protocol timed out or experienced connection failures (alert red).

These micro-states are visually tracked sequentially on the Workstation and Onboarding wizard, and used as strict guardrails to prevent executing trade tickets on disconnected charts.
