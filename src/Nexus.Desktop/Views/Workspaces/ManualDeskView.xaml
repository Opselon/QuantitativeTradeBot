<UserControl x:Class="Nexus.Desktop.Views.Workspaces.ManualDeskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Nexus.Desktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Readiness Indicator Banner -->
        <Border Grid.Row="0" Background="{Binding Mt5Trading.WarningBannerColor, FallbackValue='#C62828'}" BorderBrush="#393E46" BorderThickness="1" CornerRadius="4" Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚡" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="{Binding Mt5Trading.WarningBannerText, FallbackValue='Bridge Offline: Manual Execution Disabled'}" Foreground="White" FontSize="13" FontWeight="Bold"/>
                    <TextBlock Text="Manual executing operations are enabled. Order execution parameters (StopLoss, TakeProfit) bypass strategy automation." Foreground="#CCCCCC" FontSize="11" Margin="0,3,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <views:Mt5TradingPanel Grid.Row="2" DataContext="{Binding Mt5Trading}"/>
    </Grid>
</UserControl>
