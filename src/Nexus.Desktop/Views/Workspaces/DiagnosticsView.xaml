<UserControl x:Class="Nexus.Desktop.Views.Workspaces.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#1E1E1E" BorderBrush="#393E46" BorderThickness="1" CornerRadius="4" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0">
                <TextBlock Text="SYSTEM DIAGNOSTICS &amp; BOUNDED LOGS" FontFamily="Segoe UI Semibold" FontSize="20" Foreground="#00ADB5" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="REFRESH" Command="{Binding RefreshLogsCommand}" Background="#00ADB5" Foreground="Black" Padding="12,5" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                    <Button Content="CLEAR" Command="{Binding ClearLogsCommand}" Background="#C62828" Foreground="White" Padding="12,5" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>
            <ListView Grid.Row="1" ItemsSource="{Binding StructuredLogs}" Background="#121212" BorderBrush="#393E46" Foreground="White" Margin="0,10,0,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Timestamp" DisplayMemberBinding="{Binding TimestampUtc, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="150"/>
                        <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="100"/>
                        <GridViewColumn Header="Severity" DisplayMemberBinding="{Binding Severity}" Width="80"/>
                        <GridViewColumn Header="Direction" DisplayMemberBinding="{Binding Direction}" Width="80"/>
                        <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}" Width="450"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Border>
</UserControl>
