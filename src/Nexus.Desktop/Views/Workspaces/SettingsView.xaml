<UserControl x:Class="Nexus.Desktop.Views.Workspaces.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#1E1E1E" BorderBrush="#393E46" BorderThickness="1" CornerRadius="4" Padding="20">
        <StackPanel>
            <TextBlock Text="NEXUS TRADING PLATFORM CONFIGURATION" FontFamily="Segoe UI Semibold" FontSize="20" Foreground="#00ADB5"/>
            <TextBlock Text="Manage local database options and general application settings." FontSize="12" Foreground="#BBBBBB" Margin="0,5,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBlock Text="Active Database Provider:" FontFamily="Segoe UI Semibold" FontSize="12" Foreground="#888888" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding SelectedProvider}" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
