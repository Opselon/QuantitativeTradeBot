<UserControl x:Class="Nexus.Desktop.Views.Workspaces.Mt5BridgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E1E" BorderBrush="#393E46" BorderThickness="1" CornerRadius="4" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="NEXUS MT5 LOCALHOST BRIDGE CONTROL" FontFamily="Segoe UI Semibold" FontSize="20" Foreground="#00ADB5" Margin="0,0,0,5"/>
                    <TextBlock Text="Establish local bridge sockets, authenticate credentials, and monitor connection health." FontSize="12" Foreground="#BBBBBB"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Connection Settings -->
            <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#393E46" BorderThickness="1" CornerRadius="4" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="CONNECTION CONFIGURATION" FontFamily="Segoe UI" FontSize="14" Foreground="#EEEEEE" Margin="0,0,0,10"/>

                        <TextBlock Text="Bridge Endpoint Host:" FontFamily="Segoe UI Semibold" FontSize="12" Foreground="#888888" Margin="0,5,0,4"/>
                        <TextBox Text="{Binding Mt5BridgeHost, UpdateSourceTrigger=PropertyChanged}" Background="#222831" Foreground="White" BorderBrush="#393E46" Padding="6,4" FontSize="13" Margin="0,0,0,10"/>

                        <TextBlock Text="Bridge Endpoint Port:" FontFamily="Segoe UI Semibold" FontSize="12" Foreground="#888888" Margin="0,5,0,4"/>
                        <TextBox Text="{Binding Mt5BridgePort, UpdateSourceTrigger=PropertyChanged}" Background="#222831" Foreground="White" BorderBrush="#393E46" Padding="6,4" FontSize="13" Margin="0,0,0,15"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button Content="START BRIDGE LISTENER" Command="{Binding BridgeConnectCommand}" Background="#00ADB5" Foreground="Black" FontWeight="Bold" Padding="15,6" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                            <Button Content="STOP BRIDGE LISTENER" Command="{Binding BridgeDisconnectCommand}" Background="#393E46" Foreground="#EEEEEE" Padding="12,6" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>

                        <Border BorderBrush="#393E46" BorderThickness="0,1,0,0" Margin="0,5,0,15"/>

                        <TextBlock Text="SECURE LOGIN CREDENTIALS" FontFamily="Segoe UI" FontSize="14" Foreground="#EEEEEE" Margin="0,0,0,10"/>

                        <TextBlock Text="Account ID / Login ID:" FontFamily="Segoe UI Semibold" FontSize="12" Foreground="#888888" Margin="0,5,0,4"/>
                        <TextBox Text="{Binding LoginAccountId, UpdateSourceTrigger=PropertyChanged}" Background="#222831" Foreground="White" BorderBrush="#393E46" Padding="6,4" FontSize="13" Margin="0,0,0,10"/>

                        <TextBlock Text="Broker Password / Secret:" FontFamily="Segoe UI Semibold" FontSize="12" Foreground="#888888" Margin="0,5,0,4"/>
                        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Background="#222831" Foreground="White" BorderBrush="#393E46" Padding="6,4" FontSize="13" Margin="0,0,0,10"/>

                        <TextBlock Text="Broker Server Name:" FontFamily="Segoe UI Semibold" FontSize="12" Foreground="#888888" Margin="0,5,0,4"/>
                        <TextBox Text="{Binding BrokerServer, UpdateSourceTrigger=PropertyChanged}" Background="#222831" Foreground="White" BorderBrush="#393E46" Padding="6,4" FontSize="13" Margin="0,0,0,15"/>

                        <Button Content="SECURE AUTHORIZE" Command="{Binding BridgeLoginCommand}" Background="#00ADB5" Foreground="Black" FontWeight="Bold" Padding="15,6" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Panel: EA Installation Guide -->
            <Border Grid.Column="2" Background="#1E1E1E" BorderBrush="#393E46" BorderThickness="1" CornerRadius="4" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="EA INSTALLATION GUIDE / HELP" FontFamily="Segoe UI Semibold" FontSize="20" Foreground="#00ADB5" Margin="0,0,0,12"/>
                        <TextBlock Text="Follow these instructions to connect your MetaTrader 5 Terminal to NTE:" FontSize="12" Foreground="#BBBBBB" TextWrapping="Wrap" Margin="0,0,0,12"/>

                        <TextBlock Text="1. Open MT5 Terminal Data Folder:" FontWeight="Bold" Foreground="#00ADB5" Margin="0,0,0,3"/>
                        <TextBlock Text="   Go to File -> Open Data Folder inside MT5." FontSize="12" Foreground="#BBBBBB" Margin="0,0,0,8"/>

                        <TextBlock Text="2. Copy the Expert Advisor file:" FontWeight="Bold" Foreground="#00ADB5" Margin="0,0,0,3"/>
                        <TextBlock Text="   Copy 'NexusBridge.mq5' from the repository to the data folder's 'MQL5/Experts/' directory." FontSize="12" Foreground="#BBBBBB" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <TextBlock Text="3. Compile the EA in MetaEditor:" FontWeight="Bold" Foreground="#00ADB5" Margin="0,0,0,3"/>
                        <TextBlock Text="   Open the file in MetaEditor and press Compile (F7)." FontSize="12" Foreground="#BBBBBB" Margin="0,0,0,8"/>

                        <TextBlock Text="4. Attach the EA to a chart:" FontWeight="Bold" Foreground="#00ADB5" Margin="0,0,0,3"/>
                        <TextBlock Text="   Drag 'NexusBridge' onto any chart. Set Host='127.0.0.1' and Port=5000." FontSize="12" Foreground="#BBBBBB" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <TextBlock Text="5. Enable Algorithmic Trading:" FontWeight="Bold" Foreground="#00ADB5" Margin="0,0,0,3"/>
                        <TextBlock Text="   Click the 'Algo Trading' button on the MT5 top toolbar." FontSize="12" Foreground="#BBBBBB" Margin="0,0,0,8"/>

                        <TextBlock Text="6. Verify connection logs:" FontWeight="Bold" Foreground="#00ADB5" Margin="0,0,0,3"/>
                        <TextBlock Text="   Check the 'Journal' and 'Experts' tabs in MT5 terminal toolbox." FontSize="12" Foreground="#BBBBBB"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
