<UserControl x:Class="Nexus.Desktop.Views.Workspaces.Mt5BridgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Card Style -->
        <Style x:Key="BridgeCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderTranslucentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{DynamicResource ShadowBrush}" BlurRadius="12" Direction="270" ShadowDepth="2" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Card -->
        <Border Grid.Row="0" Style="{StaticResource BridgeCard}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="NEXUS MT5 LOCALHOST BRIDGE CONTROL" Style="{StaticResource EnterpriseHeader}" Margin="0,0,0,4"/>
                    <TextBlock Text="Establish local bridge sockets, authenticate credentials, and monitor connection health." Style="{StaticResource EnterpriseBodyText}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Columns Grid -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Connection Settings -->
            <Border Grid.Column="0" Style="{StaticResource BridgeCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="CONNECTION CONFIGURATION" Style="{StaticResource EnterpriseSubHeader}" Margin="0,0,0,12"/>

                        <TextBlock Text="Bridge Endpoint Host:" Style="{StaticResource EnterpriseLabel}" Margin="0,4,0,4"/>
                        <TextBox Text="{Binding Mt5BridgeHost, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EnterpriseTextBox}" Margin="0,0,0,12"/>

                        <TextBlock Text="Bridge Endpoint Port:" Style="{StaticResource EnterpriseLabel}" Margin="0,4,0,4"/>
                        <TextBox Text="{Binding Mt5BridgePort, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EnterpriseTextBox}" Margin="0,0,0,16"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Content="START BRIDGE LISTENER" Command="{Binding BridgeConnectCommand}" Style="{StaticResource ActionButton}" Margin="0,0,12,0"/>
                            <Button Content="STOP BRIDGE LISTENER" Command="{Binding BridgeDisconnectCommand}" Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>

                        <Border BorderBrush="{DynamicResource BorderTranslucentBrush}" BorderThickness="0,1,0,0" Margin="0,4,0,16"/>

                        <TextBlock Text="SECURE LOGIN CREDENTIALS" Style="{StaticResource EnterpriseSubHeader}" Margin="0,0,0,12"/>

                        <TextBlock Text="Account ID / Login ID:" Style="{StaticResource EnterpriseLabel}" Margin="0,4,0,4"/>
                        <TextBox Text="{Binding LoginAccountId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EnterpriseTextBox}" Margin="0,0,0,12"/>

                        <TextBlock Text="Broker Password / Secret:" Style="{StaticResource EnterpriseLabel}" Margin="0,4,0,4"/>
                        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EnterpriseTextBox}" Margin="0,0,0,12"/>

                        <TextBlock Text="Broker Server Name:" Style="{StaticResource EnterpriseLabel}" Margin="0,4,0,4"/>
                        <TextBox Text="{Binding BrokerServer, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EnterpriseTextBox}" Margin="0,0,0,16"/>

                        <Button Content="SECURE AUTHORIZE" Command="{Binding BridgeLoginCommand}" Style="{StaticResource ActionButton}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Panel: EA Installation Guide -->
            <Border Grid.Column="2" Style="{StaticResource BridgeCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="EA INSTALLATION GUIDE / HELP" Style="{StaticResource EnterpriseHeader}"/>
                        <TextBlock Text="Follow these instructions to connect your MetaTrader 5 Terminal to NTE:" Style="{StaticResource EnterpriseBodyText}" Margin="0,0,0,16"/>

                        <TextBlock Text="1. Open MT5 Terminal Data Folder:" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="   Go to File -> Open Data Folder inside MT5." Style="{StaticResource EnterpriseBodyText}" Margin="0,0,0,10"/>

                        <TextBlock Text="2. Copy the Expert Advisor file:" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="   Copy 'NexusBridge.mq5' from the repository to the data folder's 'MQL5/Experts/' directory." Style="{StaticResource EnterpriseBodyText}" Margin="0,0,0,10"/>

                        <TextBlock Text="3. Compile the EA in MetaEditor:" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="   Open the file in MetaEditor and press Compile (F7)." Style="{StaticResource EnterpriseBodyText}" Margin="0,0,0,10"/>

                        <TextBlock Text="4. Attach the EA to a chart:" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="   Drag 'NexusBridge' onto any chart. Set Host='127.0.0.1' and Port=5000." Style="{StaticResource EnterpriseBodyText}" Margin="0,0,0,10"/>

                        <TextBlock Text="5. Enable Algorithmic Trading:" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="   Click the 'Algo Trading' button on the MT5 top toolbar." Style="{StaticResource EnterpriseBodyText}" Margin="0,0,0,10"/>

                        <TextBlock Text="6. Verify connection logs:" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="   Check the 'Journal' and 'Experts' tabs in MT5 terminal toolbox." Style="{StaticResource EnterpriseBodyText}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
