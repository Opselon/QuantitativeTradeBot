<UserControl x:Class="Nexus.Desktop.Views.Workspaces.Mt5BridgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100">
    <Grid>
        <Grid.RowDefinitions>
            <!-- 1. Header Card -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <!-- 2. Dual Panel Content -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Premium Header Card -->
        <materialDesign:Card Grid.Row="0" Style="{StaticResource NtePremiumCard}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon Kind="Bridge" Width="36" Height="36" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,15,0">
                    <materialDesign:PackIcon.Style>
                        <Style TargetType="materialDesign:PackIcon">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource LightPrimaryBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:PackIcon.Style>
                </materialDesign:PackIcon>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="NEXUS MT5 LOCALHOST BRIDGE CONTROL" Style="{StaticResource NteHeadline}" Margin="0,0,0,3"/>
                    <TextBlock Text="Establish secure local socket bridges, authenticate broker terminal accounts, and monitor bidirectional pings." Style="{StaticResource NteBodyMedium}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Dual Panel workspace -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Secure Config & Authentication -->
            <materialDesign:Card Grid.Column="0" Style="{StaticResource NtePremiumCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Connection config -->
                        <TextBlock Text="BRIDGE SOCKET PARAMETERS" Style="{StaticResource NteTitleLarge}" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding Mt5BridgeHost, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NteFloatingHintTextBox}" materialDesign:HintAssist.Hint="Bridge Host IP"/>
                            <TextBox Grid.Column="2" Text="{Binding Mt5BridgePort, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NteFloatingHintTextBox}" materialDesign:HintAssist.Hint="Bridge Listener Port"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                            <Button Content="START LISTENER" Command="{Binding BridgeConnectCommand}" Style="{StaticResource NtePremiumButton}" Margin="0,0,10,0"/>
                            <Button Content="STOP LISTENER" Command="{Binding BridgeDisconnectCommand}" Style="{StaticResource NteSecondaryButton}"/>
                        </StackPanel>

                        <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource GlassBorderBrush}" Margin="0,0,0,20">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource LightGlassBorderBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <!-- Credentials -->
                        <TextBlock Text="SECURE BROKER AUTHENTICATION" Style="{StaticResource NteTitleLarge}" Margin="0,0,0,15"/>

                        <TextBox Text="{Binding LoginAccountId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NteFloatingHintTextBox}" materialDesign:HintAssist.Hint="Account ID / Login ID" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NteFloatingHintTextBox}" materialDesign:HintAssist.Hint="Broker Password / Secret Key" Margin="0,0,0,15"/>
                        <TextBox Text="{Binding BrokerServer, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NteFloatingHintTextBox}" materialDesign:HintAssist.Hint="MT5 Server Broker Name" Margin="0,0,0,25"/>

                        <Button Content="SECURE AUTHORIZE" Command="{Binding BridgeLoginCommand}" Style="{StaticResource NtePremiumButton}"/>
                    </StackPanel>
                </ScrollViewer>
            </materialDesign:Card>

            <!-- Right Panel: EA Installation Guide -->
            <materialDesign:Card Grid.Column="2" Style="{StaticResource NtePremiumCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="EXPERT ADVISOR INSTALL GUIDE" Style="{StaticResource NteTitleLarge}" Margin="0,0,0,15"/>
                        <TextBlock Text="To successfully link your MetaTrader 5 Terminal to NTE:" Style="{StaticResource NteBodyMedium}" Margin="0,0,0,15" TextWrapping="Wrap"/>

                        <StackPanel Margin="5,0">
                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="1. Open Data Folder inside MT5" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource NteTitleMedium}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource LightPrimaryBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="   Go to File -> Open Data Folder inside MT5 terminal window." Style="{StaticResource NteBodyMedium}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="2. Copy the Expert Advisor file" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource NteTitleMedium}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource LightPrimaryBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="   Copy 'NexusBridge.mq5' from the repo to data folder's 'MQL5/Experts/' directory." Style="{StaticResource NteBodyMedium}" TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="3. Compile EA in MetaEditor" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource NteTitleMedium}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource LightPrimaryBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="   Open the file in MetaEditor and press Compile (F7) with zero errors." Style="{StaticResource NteBodyMedium}"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="4. Attach EA to Chart" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource NteTitleMedium}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource LightPrimaryBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="   Drag 'NexusBridge' onto EURUSD chart. Set Host='127.0.0.1' and Port=5000." Style="{StaticResource NteBodyMedium}"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="5. Activate Algorithmic Trading" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource NteTitleMedium}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Tag}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource LightPrimaryBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="   Click 'Algo Trading' on top MT5 toolbar and check experts status." Style="{StaticResource NteBodyMedium}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
